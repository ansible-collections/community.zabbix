minor_changes:
  - added 10 retries to agent API calls to workaround connection problems on macOS
bugfixes:
  - in ``zabbix_agent_interfaces`` directly use ``zabbix_agent_listenport``, which does already contains the agent2 
    value if needed
  - fix error when ``zabbix_agent_tlsaccept`` is not set
  - fix error when ``zabbix_agent_tlsconnect`` is not set
