---
- name: Include OS-specific variables
  ansible.builtin.include_vars: "{{ ansible_facts['lsb']['id'] }}.yml"
  tags:
    - always

- name: Install the correct repository
  ansible.builtin.include_tasks: "{{ ansible_facts['lsb']['id'] }}.yml"
